import { useEffect, useState } from "react"; import { Download, ExternalLink, X } from "lucide-react";
type BeforeInstallPromptEvent=Event&{prompt:()=>Promise<void>;userChoice:Promise<{outcome:"accepted"|"dismissed";platform:string}>};
const standalone=()=>window.matchMedia("(display-mode: standalone)").matches||Boolean((navigator as Navigator&{standalone?:boolean}).standalone); const ios=()=>/iphone|ipad|ipod/i.test(navigator.userAgent);
export default function InstallApp(){const[promptEvent,setPromptEvent]=useState<BeforeInstallPromptEvent|null>(null);const[installed,setInstalled]=useState(false);const[dismissed,setDismissed]=useState(false);const[showIOS,setShowIOS]=useState(false);useEffect(()=>{if(standalone()){setInstalled(true);return}setDismissed(localStorage.getItem("world-ai:install-dismissed")==="true");const ready=(e:Event)=>{e.preventDefault();setPromptEvent(e as BeforeInstallPromptEvent)};const done=()=>{setInstalled(true);setPromptEvent(null)};window.addEventListener("beforeinstallprompt",ready);window.addEventListener("appinstalled",done);return()=>{window.removeEventListener("beforeinstallprompt",ready);window.removeEventListener("appinstalled",done)}},[]);if(installed||dismissed)return null;const install=async()=>{if(promptEvent){await promptEvent.prompt();const choice=await promptEvent.userChoice;setPromptEvent(null);if(choice.outcome==="accepted")setInstalled(true);return}if(ios())setShowIOS(true);else alert("The browser has not made the install prompt available yet. Open WORLD AI in Chrome over HTTPS, then try again.")};return <>{<div className="install-banner"><div className="install-banner-icon"><Download size={19}/></div><div className="install-banner-copy"><strong>Install WORLD AI</strong><span>{promptEvent?"WORLD AI is ready to install.":"Add WORLD AI to your device when your browser makes installation available."}</span></div><button className="install-button" onClick={()=>void install()}>Install App</button><button className="install-close" onClick={()=>{setDismissed(true);localStorage.setItem("world-ai:install-dismissed","true")}}><X size={17}/></button></div>}{showIOS&&<div className="install-ios-backdrop" onClick={()=>setShowIOS(false)}><div className="install-ios-card" onClick={e=>e.stopPropagation()}><div className="install-ios-title"><strong>Install WORLD AI</strong><button className="icon-button" onClick={()=>setShowIOS(false)}><X size={18}/></button></div><p>In Safari, tap <strong>Share</strong>, then choose <strong>Add to Home Screen</strong>.</p><div className="install-ios-note"><ExternalLink size={17}/> WORLD AI will open like an app.</div></div></div>}</>}
